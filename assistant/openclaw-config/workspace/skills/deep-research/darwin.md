# Deep Research（macOS）

- 任务目录：`$HOME/tasks/$(date +%F)-<topic>/`
- 抓取：OpenClaw `web_search` / `web_fetch`；必要时 `curl -fsSL`。
- 报告：`研究报告.md`；可选用 `office-docs` 转 docx（`$HOME/.openclaw/venv/bin/python` + pypandoc）。
- 长任务：先确认范围，再多轮检索；中间产物写入同目录 `notes/`。
